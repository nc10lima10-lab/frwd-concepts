{
  "name": "frwd-concepts-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "A static editorial portfolio for FRWD Concepts.",
  "scripts": {
    "dev": "python3 -m http.server 4174",
    "check": "node --check data/content.js && node --check scripts/home.js && node --check scripts/project.js",
    "build": "npm run check"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
